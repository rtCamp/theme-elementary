# Shared Styles

Files in this directory are **not** compiled as standalone entry points.

They are meant to be imported via `@use` or `@forward` from entry files in `frontend/`, `admin/`, or `editor/`.
